<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="style2.css" />
    <title>Bookers_wcp</title>
    <script src="https://kit.fontawesome.com/ebd5cc16fe.js" crossorigin="anonymous"></script>

  </head>

  <body>
    <h1>Books</h1>
    <table>
      <thead>
        <tr>
          <th>Titles</th>
          <th>Body</th>
        </tr>
      </thead>
      <tbody>
      <% @books.each do |book| %>
        <tr>
          <td class="book-title"><%= link_to book.title, book_path(book.id) %></td>
          <td class="book-text"><%= link_to book.body,book_path(book.id) %></td>
          <td>
            <a href="books/">Show</a>
            <a href="books/edit">Edit</a>
            <a href="books/">Destroy</a>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>

  </body>
</html>